Assuming you have node and npm installed,
(if not go to nodejs.org and follow the instructions)

We are installing `browserify`, our code 
stiching tool.  It uses the commonjs/node module 
system to package our js in a single file.

Cow Says: npm install browserify --save && ./n
